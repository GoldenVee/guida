import { Grid, GridItem, Heading } from "@chakra-ui/react";

const Notepad = () => {
  return (
    <Grid>
      <GridItem>
        <Heading>Notepad</Heading>
      </GridItem>
    </Grid>
  );
};

export default Notepad;
